sudo: false
language: python
env:
  - NODE_VERSION="16"
before_install:
  - nvm install $NODE_VERSION
python:
  - "3.8"
install: pip install tox-travis
before_script:
  - tox
script:
  - cd npm
  - npm ci
  - npm run copy:schemas
  - npm run semantic-release
